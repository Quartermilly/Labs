name = input('Enter your name: ')
for letter in name:
    print(letter.upper())  # prints the letters in name in uppercase
print(f'That was your name in uppercase vertically, {name}. Thanks for playing!')
# in the above line it doesn't have the upper part so its in whichever case the input was
